<!DOCTYPE html>
<html>
<head>
  <title><%= city.name %> - GeoGenius</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
  <nav>
    <div class="logo">🌍 GeoGenius</div>
    <div class="nav-links">
      <a href="/">← Back to Map</a>
      <% if (user) { %>
        <span>Welcome, <strong><%= user.username %></strong></span>
      <% } %>
    </div>
  </nav>

  <div class="city-page-container">
    <!-- Header with yellow border -->
    <div class="city-header-box" style="background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url('/images/<%= city.cityImage %>'); background-size: cover; background-position: center;">
      <h1><%= city.name %></h1>
      <p class="coords">📍 <%= city.lat %>, <%= city.lng %></p>
    </div>

    <!-- Grid of cards -->
    <div class="questions-grid">
      <% if (questions.length > 0) { %>
        <% questions.forEach(q => { %>
          <div class="question-card-boxed">
            <h2><%= q.text %></h2>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</body>
</html>
